import { model } from "@angular/core"

export interface productsModel {
    name: string,
    image: string,
    description: string,
    descripDetail : string,
    price: number,
    qtty:number,
    availability: Boolean

}